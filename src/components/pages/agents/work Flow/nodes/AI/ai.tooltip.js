export const aiInput = (
	<div>
		<p>
			This node will run everytime it recives an instagram message and
			sender ID, Only runs when Instagram is configure in plugins
		</p>
		<h6>This node connects with:</h6>
		<ol>
			<li>Human (plugin)</li>
			<li>ChatBot (AI)</li>
		</ol>
	</div>
);
export const aiKnowledgeRetrival = (
	<div>
		<p>
			This node will run everytime it recives an instagram message and
			sender ID, Only runs when Instagram is configure in plugins
		</p>
		<h6>This node connects with:</h6>
		<ol>
			<li>Human (plugin)</li>
			<li>ChatBot (AI)</li>
		</ol>
	</div>
);
export const aiPlugin = (
	<div>
		<p>
			This node will run everytime it recives an instagram message and
			sender ID, Only runs when Instagram is configure in plugins
		</p>
		<h6>This node connects with:</h6>
		<ol>
			<li>Human (plugin)</li>
			<li>ChatBot (AI)</li>
		</ol>
	</div>
);
export const aiExtendAgent = (
	<div>
		<p>
			This node will run everytime it recives an instagram message and
			sender ID, Only runs when Instagram is configure in plugins
		</p>
		<h6>This node connects with:</h6>
		<ol>
			<li>Human (plugin)</li>
			<li>ChatBot (AI)</li>
		</ol>
	</div>
);

export const aiSystemPrompt = (
	<div>
		<p>
			This node will run everytime it recives an instagram message and
			sender ID, Only runs when Instagram is configure in plugins
		</p>
		<h6>This node connects with:</h6>
		<ol>
			<li>Human (plugin)</li>
			<li>ChatBot (AI)</li>
		</ol>
	</div>
);

export const aiOutput = (
	<div>
		<p>
			This node will run everytime it recives an instagram message and
			sender ID, Only runs when Instagram is configure in plugins
		</p>
		<h6>This node connects with:</h6>
		<ol>
			<li>Human (plugin)</li>
			<li>ChatBot (AI)</li>
		</ol>
	</div>
);
